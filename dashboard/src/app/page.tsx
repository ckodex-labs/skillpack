import App from '../console/App';

export const metadata = {
  title: 'SkillPack · Governed Skills Console',
  description:
    'One console: registry, fleet assurance, local store, discovery, migration, and evidence — wired to the live skillpack daemon.',
};

export default function HomePage() {
  return <App />;
}
