# Simple Tool

A minimal stateless skill with no lifecycle hooks.

## Usage

Invoke directly. No installation required. Pass a `query` parameter and receive a `result` in response.

## Notes

This fixture is used in the SkillPack test suite to verify that the Lifecycle dimension checker correctly identifies the absence of lifecycle hooks in a CNSB bundle and reports an appropriate warning.
